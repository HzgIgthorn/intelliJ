<thlogic>
    <attr sel="#headId" th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#home-title" th:text="#{home.title}"/>
    <attr sel="#playlink" th:text="#{home.press}" th:href="@{__${T(academy.learnprogramming.util.GameMappings).PLAY}__}"/>
    <attr sel="#footerId" th:replace="~{fragments/footer :: footFragment}"/>
</thlogic>